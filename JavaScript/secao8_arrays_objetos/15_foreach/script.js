// Definindo um array de nomes
let nomes = ["Matheus", "Wagner", "José", "Maria"];

// Utilizando o método forEach para iterar sobre cada elemento do array
nomes.forEach(nome => {
    // Imprimindo uma mensagem no console para cada nome
    console.log("O nome é " + nome);
});
