// Criação de um objeto chamado 'obj' com três propriedades e valores associados.
let obj = {
    keys1: 1,
    keys2: 2,
    keys3: 3
};

// Exibição do objeto 'obj' no console.
console.log(obj);

// Utilização de 'Object.keys(obj)' para obter um array contendo as chaves do objeto 'obj'.
console.log(Object.keys(obj));


// o código cria um objeto chamado obj com algumas propriedades, imprime o objeto no console e, em seguida, imprime as chaves desse objeto usando Object.keys().